/target

# VaultMind index (regenerable) — ignore the DB, keep the committed config.yaml
.vaultmind/index.db
.vaultmind/index.db-shm
.vaultmind/index.db-wal
vault/.vaultmind/index.db
vault/.vaultmind/index.db-shm
vault/.vaultmind/index.db-wal

# VaultMind per-session episode transcripts (local durable capture)
vault/episodes/

*.swp
*.swo
*~
.DS_Store

# Vendored spec requirements (fetched at runtime, cached locally)
conformance/requirements.json
